define([],
	function() {
		app.controller('DashBoardController', [ '$scope','$state', function($scope, $state) {
			$scope.greeting = "Hello World.";
//			console.log(menuService.getDateTime());
		}]);
	}
);